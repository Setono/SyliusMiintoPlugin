{# @var \Setono\SyliusMiintoPlugin\Model\ErrorInterface[] data #}
{% if data|length > 0 %}
    <ul class="ui list">
        {% for error in data %}
            <li>{{ error.message|u.truncate(80) }}</li>
        {% endfor %}
    </ul>
{% else %}
    <i class="check circle green icon"></i> No errors
{% endif %}
